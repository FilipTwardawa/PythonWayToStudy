# Create a function that takes 2 arguments of type int , and then returns the
# the result of multiplying both numbers.

def get_sum_of_two_numbers(number1: int, number2: int) -> int:
    return number1 * number2


result = get_sum_of_two_numbers(2, 2)

print("The sum of the two numbers is = ", result)
